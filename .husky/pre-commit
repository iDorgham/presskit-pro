#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Run type checking
echo "🔧 Type checking..."
npm run type-check 2>/dev/null || echo "⚠️  Type checking skipped (script not found)"

echo "✅ Pre-commit checks completed!"
